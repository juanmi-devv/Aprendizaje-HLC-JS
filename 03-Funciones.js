// Funcion (en esta funcion con array fuction .push 

function tablaDeMultiplicar(valor) {
    let datos = []
    for(let numero = 0; numero < 10; numero++) {
        datos.push(valor + " X " + numero + "=" + valor*numero)
        alert(tablaDeMultiplicar)
    }
    return datos
}

// Funcion facil.
/*
function sumar(num1, num2) {
    let num3 = 10
    let resultado = num1 + num2 + num3
    return resultado
}

alert(sumar(1,3))
*/